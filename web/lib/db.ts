// Template only — no implementation yet.
// TODO: DB client. Must support: personal columns never in logs/metrics
//   (rule.md PDPA), TLS connection, credentials from env/secrets manager
//   only (never committed), and a retention/anonymise job for any table
//   holding personal data (email, delivery-channel preference).

export {};
