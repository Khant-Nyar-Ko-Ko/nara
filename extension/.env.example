# Copy to .env.local (Vite loads it automatically). Must match a pattern
# in manifest.json's host_permissions, or fetch() from the popup/background
# is blocked by CORS.
VITE_API_BASE_URL=http://localhost:3000
