// Template only — no implementation yet.
// TODO: thin client for calling the web/ Next.js backend's API routes
//   (GET /api/digest for F2, POST /api/consent for LR4, etc).
// Base URL should come from an env/build-time config, not be hardcoded.

export {};
