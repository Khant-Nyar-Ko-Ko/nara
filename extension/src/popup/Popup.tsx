// Template only — no implementation yet.
// TODO(F1): fetch the aggregated digest via lib/api.ts.
// TODO(F2): render as a scrollable, one-line-per-headline list; each row's
//   click opens the source article in a new tab.
export function Popup() {
  return <div>NaraNews popup — not yet implemented.</div>;
}
