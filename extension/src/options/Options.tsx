// Template only — no implementation yet.
// TODO(F5/LR5): topic-priority picker + delivery-channel preference
//   (popup-only / Chrome notification / email) + delivery schedule.
//   Persist to chrome.storage with a stated purpose (LR5) — preference is
//   used only to route the digest, never repurposed.
// TODO(LR1/LR4): email opt-in flow — verified address + recorded consent
//   (user_id, document_version_hash, timestamp, IP, exact button text),
//   not a bare boolean.
export function Options() {
  return <div>NaraNews settings — not yet implemented.</div>;
}
